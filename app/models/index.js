const UserSchema = require('../models/user');
const BookSchema = require('../models/book');
const SubscriptionSchema = require('../models/subscription');
const GiftSchema = require('../models/gift');

module.exports = {
    UserSchema,
    BookSchema,
    SubscriptionSchema,
    GiftSchema
}